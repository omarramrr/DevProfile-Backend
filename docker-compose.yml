services:
  postgres:
    image: postgres:16
    container_name: devprofileproject-db

    environment:
      POSTGRES_DB: devprofileproject
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: medo1407

    ports:
      - "5433:5432"

    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
